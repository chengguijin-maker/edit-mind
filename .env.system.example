# System defaults for Docker services.
# Only modify if you need custom internal paths, ports, or service configs.

# PostgreSQL connection settings for internal container communication (User and Password should be changed)
DATABASE_URL=postgresql://user:password@postgres:5432/app
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=app
PGDATA=/var/lib/postgresql/data/pgdata

# Redis connection settings for caching and job queues
REDIS_URL=redis://redis:6379
REDIS_HOST=redis

# ChromaDB settings for embeddings and semantic search
CHROMA_HOST=chroma
IS_PERSISTENT=TRUE

# ML service host for video processing and transcription
ML_HOST=ml

PROCESSED_VIDEOS_DIR=/app/data
THUMBNAILS_PATH=/app/data/.thumbnails
STITCHED_VIDEOS_DIR=/app/data/.stitched-videos

# Face recognition data directories
FACES_DIR=/app/data/.faces
UNKNOWN_FACES_DIR=/app/data/.unknown_faces
KNOWN_FACES_FILE=/app/data/.faces.json
KNOWN_FACES_FILE_LOADED=/app/data/.known_faces.json


BACKGROUND_JOBS_URL=http://background-jobs:4000
NODE_ENV=production
ANONYMIZED_TELEMETRY=FALSE

# Web application endpoint (for internal API calls)
WEB_APP_URL=http://web:3745

# Video processing concurrency defaults
MAX_CONCURRENT_TRANSCRIPTIONS=1
MAX_CONCURRENT_ANALYSES=1
ANALYSIS_SAMPLE_INTERVAL_SECONDS=5
ANALYSIS_FACE_RECOGNITION_SKIP_INTERVAL=3
ANALYSIS_DESCRIPTOR_SKIP_INTERVAL=3
ANALYSIS_TEXT_DETECTION_SKIP_INTERVAL=3
